var GenerateResult = function(){
    var nDOM = document.getElementById('nrow');
    var mDOM = document.getElementById('ncol');
    if(nDOM && mDOM){
        var n = parseInt(nDOM.value);
        var m = parseInt(mDOM.value);
    }
}